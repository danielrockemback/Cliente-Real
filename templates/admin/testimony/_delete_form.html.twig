<form method="post" action="{{ path('app_admin_testimony_delete', {'id': testimony.id}) }}" onsubmit="return confirm('Você quer excluir esse item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ testimony.id) }}">
    <button class="btn btn-danger ms-3 text-white">Excluir</button>
</form>
