{{ form_start(form) }}
<div class="row">

    <div class="mb-3 col col-12">
        <label for="{{ form.title.vars.id }}" class="form-label">Título</label>
        {{ form_widget(form.title, {'attr':{'class':'form-control'}}) }}
    </div>

    <div class="mb-3 col col-6">
        <label for="{{ form.language.vars.id }}" class="form-label">Idioma</label>
        {{ form_widget(form.language, {'attr':{'class':'form-control'}}) }}
    </div>

    <div class="mb-3 col col-12">
        {% if product_category.image %}
            <img class="mt-3" src="{{ vich_uploader_asset(product_category, 'productCategoryImage') }}" width="150px">
            <br>
            <br>
        {% endif %}

        {{ form_widget(form.imageProductFile) }}
    </div>

</div>


<button class="btn btn-info float-start text-white mt-3">Salvar</button>
{{ form_end(form) }}
