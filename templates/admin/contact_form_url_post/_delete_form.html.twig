<form method="post" action="{{ path('app_admin_contact_form_url_post_delete', {'id': contact_form_url_post.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contact_form_url_post.id) }}">
    <button class="btn">Delete</button>
</form>
