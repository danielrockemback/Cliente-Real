{{ form_start(form) }}

<div class="mb-3">
    <label for="{{ form.ga4.vars.id }}" class="form-label">Ga4</label>
    {{ form_widget(form.ga4, {'attr':{'class': 'form-control', 'rows': 10, 'style': 'font-family: monospace' }}) }}
</div>

<div class="mb-3">
    <label for="{{ form.tagsGoogleAds.vars.id }}" class="form-label">Google ADS</label>
    {{ form_widget(form.tagsGoogleAds, {'attr':{'class': 'form-control', 'rows': 10, 'style': 'font-family: monospace' }}) }}
</div>

<div class="mb-3">
    <label for="{{ form.pixelMetaAds.vars.id }}" class="form-label">Meta ADS</label>
    {{ form_widget(form.pixelMetaAds, {'attr':{'class': 'form-control', 'rows': 10, 'style': 'font-family: monospace' }}) }}
</div>

<button class="btn btn-info float-start text-white">Salvar</button>

{{ form_end(form) }}