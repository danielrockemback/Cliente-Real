<form method="post" action="{{ path('app_admin_product_property_delete', {'id': product_property.id}) }}" onsubmit="return confirm('Você quer excluir este item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product_property.id) }}">
    <button class="btn btn-danger ms-3 text-white">Excluir</button>
</form>
