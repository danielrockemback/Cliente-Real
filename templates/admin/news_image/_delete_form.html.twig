<form method="post" action="{{ path('app_admin_news_image_delete', {'id': news_image.id, 'newsId': news.id}) }}" onsubmit="return confirm('Você quer excluir esse item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ news_image.id) }}">
    <button class="btn btn-danger ms-3 text-white">Excluir</button>
</form>
