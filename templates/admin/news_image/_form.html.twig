{{ form_start(form) }}
<div class="row">
    <div class="mb-3 col col-12">
        <label for="{{ form.title.vars.id }}" class="form-label">Título da Imagem</label>
        {{ form_widget(form.title, {'attr':{'class':'form-control'}}) }}
    </div>

    <div class="mb-3 col col-12">
        <label for="{{ form.alt.vars.id }}" class="form-label">Alt da Imagem</label>
        {{ form_widget(form.alt, {'attr':{'class':'form-control'}}) }}
    </div>

    <div class="mb-3 col col-6">
        {% if news_image.image %}
            <img class="mt-3" src="{{ vich_uploader_asset(news_image, 'imageFile') }}" width="150px">
            <br>
            <br>
        {% endif %}
        {{ form_widget(form.imageFile) }}
    </div>

</div>

<button class="btn btn-info float-start text-white">Salvar</button>
{{ form_end(form) }}
