{% extends 'base.html.twig' %}

{% set languagesEnum = enum('LanguageEnum') %}

{% block title %}Imagens do Produto{% endblock %}

{% block javascripts %}
    <script
            src="https://code.jquery.com/jquery-3.7.1.min.js"
            integrity="sha256-/JqT3SQfawRcv/BIHPThkBvs0OEvtFFmqPF/lYI/Cxo="
            crossorigin="anonymous"></script>
    <script>

        $(document).ready(function () {
            $('#tabela').DataTable({
                "lengthMenu": [[ 10, 25, 50, 100,-1], [ 10, 25, 50, 100,"Todos"]],
                "language": {
                    "lengthMenu": "Mostrando _MENU_ registros por página",
                    "zeroRecords": "Nada encontrado",
                    "info": "Mostrando página _PAGE_ de _PAGES_",
                    "infoEmpty": "Nenhum registro disponível",
                    "infoFiltered": "(filtrado de _MAX_ registros no total)",
                    "search": " Pesquisar",

                    "paginate": {
                        "first": "Primeira",
                        "last": "Última",
                        "next": "Próxima",
                        "previous": "Anterior"
                    },
                }
            });
        });
    </script>
{% endblock %}

{% block body %}
    <link rel="stylesheet" href="https://cdn.datatables.net/2.3.3/css/dataTables.dataTables.css" />
    <script src="https://cdn.datatables.net/2.3.3/js/dataTables.js"></script>

    <h1>{{ product.title }}</h1>
    <h5>Imagens do Produto</h5>

    <div class="row text-end">
        <div class="col mb-3">
            <a href="{{ path('app_admin_product_image_new', {'productId': product.id}) }}"
               type="button"
               class="btn btn-success text-white">Novo</a>
        </div>
    </div>

    <div class="card mb-5">
        <h5 class="card-header">
            Imagens do Produto
        </h5>
        <div class="card-body">
            <table class="table" id="tabela">
                <thead>
                    <tr>
                        <th class="text-center">Produto</th>
                        <th class="text-center">Imagem</th>
                        <th class="text-center">Título</th>
                        <th class="text-center">Alt</th>
                        <th class="text-center"></th>
                    </tr>
                </thead>

                <tbody>
                {% for product_image in product_images %}
                    <tr>
                        <td class="text-center">{{ product_image.product.title }}</td>
                        <td class="text-center">
                            {% if product_image.imageFile is not null %}
                                <img src="{{ vich_uploader_asset(product_image, 'imageFile') }}" width="75px">
                            {% endif %}
                        </td>
                        <td class="text-center">{{ product_image.imageTitle }}</td>
                        <td class="text-center">{{ product_image.imageAlt }}</td>
                        <td class="text-center">
                            <a class="btn btn-primary"
                               href="{{ path('app_admin_product_image_edit', {'id': product_image.id, 'productId': product.id}) }}">
                                Editar
                            </a>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>

    <a class="btn btn-secondary" href="{{ path('app_admin_product_index') }}">Ver produtos</a>
{% endblock %}
