{{ form_start(form) }}
<div class="row">

    <div class="mb-3 col col-12">
        <label for="{{ form.product.vars.id }}" class="form-label">Produto</label>
        {{ form_widget(form.product, {'attr':{'class':'form-control'}}) }}
    </div>

    <div class="mb-3 col col-12">
        <label for="{{ form.imageTitle.vars.id }}" class="form-label">Título da Imagem</label>
        {{ form_widget(form.imageTitle, {'attr':{'class':'form-control'}}) }}
    </div>

    <div class="mb-3 col col-12">
        <label for="{{ form.imageAlt.vars.id }}" class="form-label">Alt da Imagem</label>
        {{ form_widget(form.imageAlt, {'attr':{'class':'form-control'}}) }}
    </div>

    <div class="mb-3 col col-6">
        {% if product_image.image %}
            <img class="mt-3" src="{{ vich_uploader_asset(product_image, 'imageFile') }}" width="150px">
            <br>
            <br>
        {% endif %}
        {{ form_widget(form.imageFile) }}
    </div>

</div>

<button class="btn btn-info float-start text-white mt-3">Salvar</button>
{{ form_end(form) }}
